# AI Operations Center - Backend Dependencies
# Python 3.10+ compatible (tested on 3.11, 3.12, 3.14)

# ── Core API ──────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic>=2.7.4
python-multipart==0.0.9
python-dotenv==1.0.1

# ── Async HTTP ────────────────────────────────────────────────────────────────
httpx>=0.27.0

# ── LLM / Agent Frameworks ───────────────────────────────────────────────────
langgraph>=0.2.28
openai>=1.30.0           # Used as Groq-compatible client
groq>=0.9.0              # Official Groq SDK
google-genai>=2.8.0      # Google AI Studio / Gemini (Python 3.14 compatible)

# ── Vector DB ─────────────────────────────────────────────────────────────────
qdrant-client>=1.12.0

# ── Embeddings & Reranking (local, free, no API key) ─────────────────────────
sentence-transformers>=2.7.0   # Embeddings (all-MiniLM-L6-v2) + Reranker (ms-marco)
# torch is installed automatically as a sentence-transformers dependency

# ── Document Upload (PDF support) ─────────────────────────────────────────────
PyPDF2>=3.0.0            # PDF text extraction for knowledge base upload

# ── Observability (Optional — comment out if no Langfuse keys) ────────────────
# langfuse>=3.0.0        # Uncomment + set LANGFUSE_PUBLIC_KEY / LANGFUSE_SECRET_KEY
